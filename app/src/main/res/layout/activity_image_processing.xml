<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center">

    <!-- ImageView for displaying the selected image -->
    <ImageView
        android:id="@+id/image_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone"/>

    <!-- LinearLayout to hold the TextView and ImageButton together -->
    <LinearLayout
        android:layout_width="221dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <!-- TextView for displaying "请选择一张图片" -->
        <TextView
            android:id="@+id/tv_select_image_prompt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="8dp"
            android:text="请点击下方按钮选择一张图片"
            android:textColor="@android:color/black"
            android:textSize="16sp" />

        <!-- ImageButton for selecting an image -->
        <ImageButton
            android:id="@+id/btn_select_image"
            android:layout_width="152dp"
            android:layout_height="120dp"
            android:contentDescription="选择图片"
            android:src="@drawable/ic_picture" />
    </LinearLayout>

    <!-- LinearLayout for action buttons (cancel and confirm) -->
    <LinearLayout
        android:id="@+id/action_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal"
        android:layout_gravity="bottom|center"
        android:visibility="gone"
        android:padding="16dp">

        <Button
            android:id="@+id/btn_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="取消"
            android:textSize="18sp"
            android:padding="16dp"
            android:layout_marginEnd="32dp"/>

        <Button
            android:id="@+id/btn_confirm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="保存"
            android:textSize="18sp"
            android:padding="16dp"/>
    </LinearLayout>
</FrameLayout>